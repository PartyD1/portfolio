# PR 12: the records

Branch: `mobile/12-records`. Size: S. Motion skill: no. `/polish`: no (docs and captures). Real device: no.

Depends on everything else merged. Last by definition: `DESIGN.md` is regenerated from what shipped, never hand-patched, and the capture set describes the final site.

## 1. Regenerate DESIGN.md

Run the Impeccable documenter (`/impeccable document`). In this environment the shipped documenter agent is Codex-only, so substitute a fresh `general-purpose` agent pointed at `reference/degraded/documenter.md` with the full input packet, and say so in one line (memory: `impeccable-workflow-quirks`). Call the scripts by absolute path into the main checkout (`/Users/ParthDoshi/csProjects/portfolio/.claude/skills/impeccable/scripts/...`) with cwd in the worktree.

Read the result against this list; each item must be described as it ships, in the document's own voice, not pasted from these plans:

- The breakpoint set: 1000, 900, **760 (the phone)**, 400, 360; no 720, no 640.
- The Px-Bound Display Rule extended with the phone form of the hero invariant: the name line at most 97% of the column from 280 to 760 at 16px and 20px root; two lines, never three.
- The phone type ramp (the table in `pr-01-foundations.md` section 3.4 and the per-PR values) and the 14px floor.
- Safe-area tokens and where they apply; `viewport-fit=cover`.
- Tap behaviour: no platform highlight, `touch-action: manipulation` on controls.
- The card on phones: chip, full-width text, the corner cue under `(hover: none)`, the flagship's cover band; one size (52px) for a corner circle on a phone.
- The bottom sheet: geometry, close paths, the email first in its foot, the keycaps hidden on touch, the motion values as finally tuned (PR 09 writes them back).
- The flow's phone form as a named amendment (the Rail Rule: under 900px a chain hangs off one rail and the index sits on it).
- The slideshow's peek and the dialog; the Opaque-Media Rule unchanged.
- The copy control beside the address.
- Motion on phones: the table from `pr-09-motion.md` section 1 in its final state, the reduce table from section 8, and the decisions (unveil off on phones, drag-to-dismiss deferred, the wash parallax shipped or not and why).
- The blur budget rule (blur only on the wash) and the count.
- Reduced transparency and forced colours.
- The theme default and the `theme-color` sync.
- The measured contrast floor after PR 11, both themes, at 390 and 1440.

`.impeccable/design.json` is regenerated by the same command; do not edit it.

## 2. CLAUDE.md

Keep it short; it is a guide, not a record.

- **Architecture**: `Shell.tsx` (the sheet is a left drawer above 760px and a bottom sheet under it, by CSS); `Slideshow.tsx` (the tall-capture dialog); `CopyEmail.tsx` (client, the one clipboard call); `Reveal.tsx` (phone root margin).
- **Styling**: replace "breakpoints at 720px (hero), 760px (grid/cards/section heads), 640px (nav)" with the five-value set and the sentence "phone rules live in `(max-width: 760px)` blocks; a new phone rule joins one of them".
- **Motion**: add "phones: no ambient drift and no spotlight by rule; press states on every pressable; the reduce table in DESIGN.md".
- **Verification**: a "Phones" paragraph: the harness command, the `--no-save` install, port 3111 from a worktree, the shared gates in `.impeccable/plans/mobile-overhaul/SEQUENCE.md` section 5, and the real-device checklist pointer.
- **Product constraints**: nothing changes.

## 3. Surface briefs

- `.impeccable/surfaces/homepage.md`: a FIRST VIEWPORT paragraph for **390 x 844** (the two-line name low on the screen, the typewriter, the three facts, the cue, WORK and the top of the flagship card in the fold) beside the existing 1440 one; a line under Direction contract, FORM, naming the bottom sheet; the hero invariant's phone form under FINISH.
- `.impeccable/surfaces/case-study.md`: FIRST VIEWPORT at 390 (back link, name at 34px, tagline, the live pill, a dense tool row, the meta line; "How it works" arriving); the Rail Rule as a named amendment; the slideshow's peek and dialog under the section that already describes it.
- `PRODUCT.md`: unchanged. No product fact changed in this programme.

## 4. The capture set

```bash
node scripts/mobile-capture.mjs --out .impeccable/review/mobile --widths 320,390,430 --themes light,dark --landscape --tablet --menu --motion
```

Commit the folds, the sheets, the menu, the motion probe and `metrics.json`. Keep `.impeccable/review/mobile-baseline-2026-09-05/` as the before. Update `.impeccable/review/manifest.json`: add the phone matrix to `capture`, and these gates with their measured values:

| Gate | Value to record |
|---|---|
| `nameLineInvariant` | worst percentage of the column across the ten widths, both roots |
| `foldPeek` | card top at 390 x 844, 320 x 568, 430 x 932 |
| `textFloor` | smallest sampled run on each route |
| `targets` | smallest interactive target on each route |
| `blurBudget` | surfaces per route at 390 |
| `contrastPhone` | worst p20 per theme at 390 (from PR 11) |
| `motionAtRest` | the running set at 390 with motion on |

## 5. Close out the plan

In `.impeccable/plans/mobile-overhaul/README.md`, fill the Targets table's third column with the final numbers and change the status line to **SHIPPED** with the merge dates. Do not delete the plan; it is the record of why.

## 6. Gate

- [ ] `npm run build`, detector 0 (the captures and docs do not change code, but the sidecar regeneration touches `.impeccable`).
- [ ] `DESIGN.md` diff read end to end; every item in section 1 present; no em dashes.
- [ ] `CLAUDE.md` describes the shipped site; every command in it runs.
- [ ] The review folder holds the new phone set; `manifest.json` parses.
- [ ] README targets filled; the memory note for this programme updated (the harness location, the plan folder, the decisions that were flipped, if any).

Commit title: `Records: DESIGN.md regenerated for the phone, CLAUDE.md and the briefs updated, phone captures`.
